export type Keybinds = {
  w: { pressed: boolean };
  s: { pressed: boolean };
  a: { pressed: boolean };
  d: { pressed: boolean };
  terminate: { pressed: boolean };
  zoom: { pressed: boolean };
};

export type MovementKey = "w" | "a" | "s" | "d";
